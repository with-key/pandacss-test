{
  "name": "biskit",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "styled-system/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean"
  },
  "dependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.12.2"
}
